[package]
name = "magictrainer"
version = "0.1.0"
description = "MagicTrainer — MTG deck strategy visualizer and trainer"
authors = ["William Fischer <Goob Entertainment Co.>"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "magictrainer_lib"
crate-type = ["staticlib", "cdylib", "rlib"]
# Rust sources live in ./rust (not ./src) so the repo has no duplicate folder names.
path = "rust/lib.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"


# Read the optimization guideline for more details: https://tauri.app/concept/size/#cargo-configuration
[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true

[[bin]]
name = "magictrainer"
path = "rust/main.rs"
